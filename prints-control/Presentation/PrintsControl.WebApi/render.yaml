services:
  - type: web
    name: printsControl-api
    env: docker
    plan: free
    dockerfilePath: Presentation/PrintsControl.WebApi/Dockerfile
    branch: main
    autoDeploy: true
    routes:
      - type: web
        source: /
        target: /
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
    mounts:
      - type: volume
        name: sqlite-data
        path: /app/data
